#  docker run -ti --rm -p 8000:5000 libretranslate/libretranslate --load-only "en,es,fr"
# docker run --rm -p 8000:5000 libretranslate

# FROM libretranslate/libretranslate:latest

# EXPOSE 5000

# CMD ["--load-only", "en,es,fr"]
